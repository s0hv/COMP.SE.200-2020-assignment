import chunk from "../src/chunk.js";

describe('chunk', () => {
    it('Should return the split array',()=>{
    //    console.log(chunk(['a', 'b', 'c', 'd'],2));
  //    expect(chunk(['a', 'b', 'c', 'd'], 2)).to.deep.equal([ ['c', 'd']]);
  //    expect(chunk(['a', 'b', 'c', 'd'], 3)).to.equal([['a', 'b', 'c'], ['d']]);
  //    expect(chunk(['a', 'b'], 2)).to.equal([['a'], ['b']]);
    });
/*
    it('Should accept array only to split the array.',()=>{
      expect(chunk('4.006)')).to.be.NaN;
    });

    it('Should not accept array of undefined value to split the array.',()=>{
      expect(chunk(undefined)).to.be.NaN;
    });
    */
});